# Aegis OS — plymouth daemon configuration.
# Theme=aegis makes plymouth-set-default-theme (and the mkinitcpio plymouth
# hook, which reads the same file at image build time) bundle the animated
# Aegis shield splash into every initramfs, live and installed alike.
[Daemon]
Theme=aegis
ShowDelay=0
