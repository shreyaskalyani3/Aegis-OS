# Aegis OS — filesystem mount options written to /etc/fstab
---
mountOptions:
  default: defaults,noatime
  btrfs: defaults,noatime,compress=zstd
  btrfs_swap: defaults,noatime
ssdExtraMountOptions:
  btrfs: discard=async
crypttabOptions: luks
